---
interface Props {
	title: string;
}

const { title } = Astro.props;
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { astroI18n, t, l } from "astro-i18n";
---

<!doctype html>
<html lang={t("index/_lang")}>
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={t("index/description")} />
		<meta name="keywords" content="nishant, suria, ns dawn, n.s. dawn, ucsc, game designer, web designer, front end, ns暁, 二シャント, スーリア">
		<meta name="author" content="Nishant Suria" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.png" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preload" href="/assets/fonts/Fira_Code/FiraCode-VariableFont_wght.ttf" as="font" type="font/ttf" crossorigin>
		<!-- 
		<link rel="preload" href="/assets/fonts/M_PLUS_1p/MPLUS1p-Regular.ttf" as="font" type="font/ttf" crossorigin>
		<link rel="preload" href="/assets/fonts/M_PLUS_1p/MPLUS1p-Bold.ttf" as="font" type="font/ttf" crossorigin>
		-->
		<link rel="preload" href="/assets/fonts/Noto_Emoji/NotoEmoji-VariableFont_wght.ttf" as="font" type="font/ttf" crossorigin>
		<script src="/assets/js/konami_code.js" defer></script>
		<title>{title}</title>
		<slot name="head"/>
	</head>
	<body>
		<div aria-hidden hidden style="display:none">{t("index/ai-summary")}</div>
		<Header />
		<slot />
		<Footer />
	</body>
</html>
<style is:global>


	@font-face {
		font-family: 'Fira';
		src: url("/assets/fonts/Fira_Code/FiraCode-VariableFont_wght.ttf");
	}
	@font-face {
		font-family: 'MPlus';
		src: url("/assets/fonts/M_PLUS_1p/MPLUS1p-Regular.ttf");

	}
	@font-face {
		font-family: 'MPlus';
		font-weight: bold;
		src: url("/assets/fonts/M_PLUS_1p/MPLUS1p-Bold.ttf");
	}
	@font-face {
		font-family: 'NotoEmoji';
		src: url("/assets/fonts/Noto_Emoji/NotoEmoji-VariableFont_wght.ttf");
	}
	:root {
		background-color: var(--rgb-bg-from);
	}
	body {
		margin: 0;
		width: 100vw; 
		background: linear-gradient(to bottom, var(--rgb-bg-from), var(--rgb-bg-to));
	}
	
	body > * {
		margin: 1rem;
	}
	h1 {
		font-size: 2.5rem;
		line-height: 3rem;
		text-align: center;
		margin: 0 1rem;
		font-family: 'Fira', 'NotoEmoji', monospace, system-ui;
	}
	h2 {
		font-size: 2rem;
		line-height: 2rem;
		font-family: 'Fira', 'NotoEmoji', monospace, system-ui;
	}
	h3 {
		font-size: 1.5rem;
		line-height: 1.5rem;
		font-family: 'Fira', 'NotoEmoji', monospace, system-ui;
	}

	main {
        display: flex;
        flex-direction: column;
    }
    .card-wrapper {
        display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    }
	.card-wrapper.vertical {
		grid-template-columns: 1fr;
		grid-template-rows: repeat(auto-fit, minmax(0px, 1fr));
	}
    section.card {
        background-color: var(--rgb-bg-to);
        margin: 0.5rem;
        padding: 0.5rem 1rem;
        outline: 0.1em var(--rgb-accent-to) solid;
        border-radius: 50px 0 50px 0;
    }

	:root {
		font-size: clamp(12px, 2.5vw, 20px);
		display: flex;
		justify-content: center;

		--rgb-bg-from: #1d1812;
		--rgb-bg-to: #000000;
		--rgb-text: #ffffff;
		--rgb-accent-from: #ffbd39;
		--rgb-accent-to: #c6690c;
		--rgb-code-bg: #220101;
		--rgb-text-gray: #8b8b8b;
		--rgb-text-warning: #e33c3c;

		font-family: Verdana, MPlus, NotoEmoji, system-ui;
		color: var(--rgb-text);
		line-height: 1.15rem;
	}
	html {
		min-height: 100vh;
		width: max-content;
		overflow-x: hidden;
		scrollbar-gutter: stable;
  	}

	img { 
		image-rendering: optimizeSpeed;             /* STOP SMOOTHING, GIVE ME SPEED  */
		image-rendering: -moz-crisp-edges;          /* Firefox                        */
		image-rendering: -o-crisp-edges;            /* Opera                          */
		image-rendering: -webkit-optimize-contrast; /* Chrome (and eventually Safari) */
		image-rendering: pixelated;                 /* Universal support since 2021   */
		image-rendering: optimize-contrast;         /* CSS3 Proposed                  */
		-ms-interpolation-mode: nearest-neighbor;   /* IE8+                           */
	}		
	a {
		all: unset;
		text-decoration: underline solid;
		color: var(--rgb-text);
		
	}
	a:hover {
		text-decoration: 0.1em solid underline;
		cursor: pointer;
		transform: scale(1.1);
		color: var(--rgb-accent-from);
	}
	/* width */
	::-webkit-scrollbar {
		width: 0.5rem;
	}

	/* Track */
	::-webkit-scrollbar-track {
		background: var(--rgb-bg-from);
	}

	/* Handle */
	::-webkit-scrollbar-thumb {
		background: var(--rgb-accent-to);
		border-radius: 0 10px 0 10px;
	}

	/* Handle on hover */
	::-webkit-scrollbar-thumb:hover {
		background: var(--rgb-accent-from);
	}

	code {
		background-color: var(--rgb-code-bg);
		font-family: 'Fira', monospace, system-ui;
		border-radius: 5px;
		padding: 0.5rem 1rem;
		outline: var(--rgb-text) 0.1rem solid;
		display: block;
		text-align: start;
	}
	span.code-comment {
        color: var(--rgb-text-gray);
    }
	noscript.warning {
		color: var(--rgb-text-warning);
		display: block;
		padding: 0.25rem 1rem;
		outline: var(--rgb-text-warning) 0.1rem dashed;
		border-radius: 20px 0 20px 0;
	}
</style>
